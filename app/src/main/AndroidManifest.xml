<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.dicoding.moviecataloguerv"
    tools:ignore="GoogleAppIndexingWarning, AllowBackup">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <permission
        android:name="com.dicoding.moviecataloguerv.READ_DATABASE"
        android:protectionLevel="normal" />
    <permission
        android:name="com.dicoding.moviecataloguerv.WRITE_DATABASE"
        android:protectionLevel="normal" />

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.NewReleaseActivity" />
        <activity android:name=".MainActivity" />

        <receiver android:name=".widget.FavoriteMovieWidget">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/favorite_image_widget_info" />
        </receiver>
        <receiver
            android:name=".notification.ReminderReceiver"
            android:enabled="true"
            android:exported="true" />

        <activity android:name=".activity.SettingActivity" />
        <activity android:name=".activity.SearchActivity" />
        <activity android:name=".activity.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.MovieDetailActivity" />
        <activity android:name=".activity.TvShowDetailActivity" />

        <service
            android:name=".widget.StackWidgetService"
            android:permission="android.permission.BIND_REMOTEVIEWS" />

        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@mipmap/ic_launcher" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/colorAccent" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:resource="@string/default_notification_channel_id" />

        <service android:name=".notification.DailyReminderService">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <provider
            android:name=".contentprovider.MovieFavoriteProvider"
            android:authorities="com.dicoding.moviecataloguerv"
            android:exported="true"
            android:readPermission="com.dicoding.moviecataloguerv.READ_DATABASE"
            android:writePermission="com.dicoding.moviecataloguerv.WRITE_DATABASE" />
    </application>

</manifest>